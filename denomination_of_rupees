//Denomination of notes 
#include <stdio.h>
int main() {
    int amount, five_hundred, two_hundred, one_hundred, ten, five, two, one;
    //input of total amount
    printf("Enter total amount: ");
    scanf("%d", &amount);
    //divide it by 500
    five_hundred = amount / 500;
    amount %= 500;//print remainder if 500 > 0
    if(five_hundred > 0)
    {
        printf("Notes of 500: %d\n", five_hundred);
    }
    //divivde it by 200
    two_hundred = amount / 200;
    amount %= 200;//print remainder if 200 > 0
    if(two_hundred > 0)
    {
        printf("Notes of 200: %d\n", two_hundred);
    }
    //divide by 100
    one_hundred = amount / 100;
    amount %= 100;//print remainder if 100 > 0
    if(one_hundred > 0)
    {
        printf("Notes of 100: %d\n", one_hundred);
    }
    //devide by 10
    ten = amount / 10;
    amount %= 10;//print remainder if 10 > 0
    if(ten > 0)
    {
        printf("Notes of 10: %d\n", ten);
    }
    //devide by 5
    five = amount / 5;
    amount %= 5;//print remainder if 5 > 0
    if(five > 0)
    {
        printf("Notes of 5: %d\n", five);
    }
    //divide by 2
    two = amount / 2;
    amount %= 2;//print remainder if 2 > 0
    if(two > 0)
    {
        printf("Coin of 2: %d\n", two);
    }
    //divide by 1
    one = amount / 1;
    amount %= 1;//print remainder if 1 > 0
    if(one > 0)
    {
        printf("Coin of 1: %d\n", one);
    }
    return 0;
}
